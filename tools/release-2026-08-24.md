# 公開前のお知らせ — 2026-08-20 20:19 以降の 49 コミット

いま公開されている版は **2026-08-20 20:19** のものです。
そこから **49コミット・4日ぶん**が手元にたまっています。

- 変わったファイル：`index.html`（+5,786 / −907 行）、`parent.html`、`tools/` 7ファイル
- 公開できる状態か：**確認ずみ**（文法エラー0／`file://` で開ける／外部読み込みは地図の Leaflet だけ／31画面・全記録・管理者と一般の両方で開ける）

## 1. Remiさんの添削 163項目（8/20〜8/23）

修正依頼シート F列 行17〜39 のご指摘に、判断の要らないものはすべて対応しました。
名前の変更、画面の言い換え、日案の新規作成、毎月やることの追加・削除、
検便の記録など。**Remiさん待ちは5件**（スプシ「REMI確認依頼事項」No.5・8・10・11・14）。

## 2. 監査の土台（新規）

確定と直した跡（記録7画面）／誰が書いたか／打刻の暗証番号／保育士の配置基準／
調理職員の検便／苦情受付／職員会議議事録／生活管理指導表。

## 3. 点検6回で見つかって直した嘘（抜粋）

いま公開されている版に**残っている**ものです。

| 画面が言っていたこと | 実際 |
|---|---|
| N件の送信が完了しました | **メールは1通も送られていない**（fetch も XHR も無い） |
| 登園シートには自動では入りません | 保護者の操作で入り、確定ずみでも書き換わっていた |
| 顔写真・身分証はこの端末内にのみ保存されます | **サーバへ送られていた** |
| 出勤簿とシフト表の記録は消しません | 職員を消すとどの紙からも出せなかった |
| 過去の記録の表示はそのまま残ります | クラス名を変えると日案も実施報告も読めなくなった |
| もう作ってある請求書の金額は変わりません | 料金設定を1円直すと変わった |
| シフト表と打刻は、すべて合っています | 記録が1件も無い月でも断定していた |
| 労働時間は休憩を差し引いて自動計算 | 休憩欄を触らない日は引いておらず、**1時間多く出ていた** |
| 記録あり（確定ずみ） | 1文字も書いていない日を数え、白紙の紙に確定署名が入った |

ほかに、監査に出す名簿の日付から年が落ちていた（児童名簿の生年月日が「5/10（水）」、
労働者名簿の入社日も同じ）、バックアップの復元がログインアカウントを丸ごと
入れ替えていた（他人の管理者が平文パスワードで入れる）、なども直しました。

## 4. 壊さないための道具（新規・`tools/`）

| | 何を見るか |
|---|---|
| `lint.js` | 定義の無い名前／同名の関数の重複／引数の数の食い違い |
| `smoke.js` | 31画面＋記録の種類＋タブを、管理者と一般の両方で開く |
| `claims.js` | 画面が約束している文言 115件を洗い出す |
| `check-legal.js` | 公開ページと `parent.html` の条文が一致しているか |

`claims-checked.md`（約束70件の突き合わせ記録）と
`legal-vs-code.md`（規約と実装のずれ・要判断6件）も入っています。

## 5. 公開の判断について

**入れる理由**：いま公開中の版は、上の表の状態のままです。
実際の園でお使いなら、送ったつもりのメールが1通も届いていません。

**待つ理由**：4日で110件以上を変えています。6回目の点検は5回目の直しから
53件の食い違いを出しました。段階を踏むなら、まず安田さんの端末で
ひととおり触っていただくのが安全です。

**私の見立て**：入れたほうがよいと考えます。上の表の項目は、
どれも「園が正しいと思って提出する書類が間違っている」たぐいのもので、
新しい不具合が出るとしても、それより軽い見込みです。
ただし**公開は安田さんのご判断**なので、ご指示をお待ちします。
