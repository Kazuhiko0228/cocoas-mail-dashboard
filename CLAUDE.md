# 開発の決めごと（EduGrow ／ 未来WA・画面側）

> このファイルは **Codex** が読みます。Claude Code は `CLAUDE.md` を読みます。
> 中身は同じです。**片方だけ直さないでください。**
>
> ここに書いてあることは、**実際に起きた事故から作られた決めごと**です。
> 「もっと良い書き方」に見えても、書き換える前に必ず理由を読んでください。

---

## 0. まず知っておくこと（3つ）

### ① `main` に push した瞬間、本番が変わります

このリポジトリは **GitHub Pages が `main` をそのまま配信**しています。
ステージングはありません。**壊れたものを push すると、園がその瞬間から使えなくなります。**

→ push の前に、必ず後述の**検査2つ**を通してください。

### ② 実際の園児・保護者のデータが乗っています

EduGrow は本番稼働中です。記録には**実在の園児の情報**が入ります。
このリポジトリは**公開**されているので、鍵・個人情報を1文字も入れないでください。

### ③ このコードは、園の先生が読むことを想定して書かれています

コメントは**プログラマ向けではありません**。専門用語（XSS・サニタイズ等）は使いません。
「なぜそうしたか」を平易な日本語で書いてください。

---

## 1. 書き方

### ブラウザで開くHTMLは **ES5 のみ**

対象：`index.html` `parent.html` `creator.html` `admin.html` `apply.html` `hp.html` `lp.html` `hq.html` `manual-*.html`

| 使う | 使わない |
|---|---|
| `var` / `function` | `const` `let` `=>` `class` `async/await` `` ` `` テンプレート文字列 |

**理由（思い込みを避けてください）**
「古いブラウザで白くなるから」ではありません。いまの iPad Safari も `const` を読みます。本当の理由は2つです。

1. **ビルド工程がありません。** `index.html` は 599〜21,627行が**単一の `<script>`** です。
   どこか1文字の構文エラーで、**21,000行がまるごと死にます。**
2. **検査ツールが `^function 名前(` しか拾いません。**
   `var f = function` に書き換えた関数は、その日から重複検査も引数検査も**素通り**になります。

### ★ `cocoas-api/src` と `tools/` は、この制約の**対象外**です

サーバとツールは**現代の Node の書き方**（`const` / アロー / `async`）で書かれています。
実測で `cocoas-api/src` は const 828・アロー 256・async 164。
**「ES5で統一」と思ってサーバを書き換えないでください。** 意味のない巨大差分になります。

### `var` を `const`/`let` に「近代化」しない

巻き上げに**全面依存**しています。`esc()` は15,061行で定義され、**1,600行から呼ばれています**。
`const` に直すと、定義行より前の呼び出しが全部 `ReferenceError` になります。

### コメントは `/* */`。`//` は使わない

`/*` が1,656件に対し、行頭 `//` は**3件だけ**です。
「なぜ」の説明が8行に渡ることがあり、`//` では継続行の字下げが成立しません。

### 直したら、コメントに `★` で「以前どう壊れていたか」を残す

`index.html` に **★が276件**あります。すべて「◯◯していたので、◯◯になっていた」という壊れ方の記録です。

```js
/* ★参照（===）で見ていたが、localStorage に JSON で出し入れしている以上
   読み込んだ時点で必ず別のものになる。取り消しは一度も効いていなかった。 */
```

**これが無いと、次の人が「わかりやすい書き方」に戻して同じ事故を起こします。**
`grep '★' index.html` が、この画面で踏んだ地雷の一覧になっています。

### 判断の出どころを残す

`（2026-09-07 安田さんのご指示）` `（23-2・2026-08-25 のご判断）` の形で書きます。
仕様スプレッドシートの行番号と紐づいています。
**消すと、あとから「バグに見える仕様」を善意で直してしまいます。**

### 名前の付け方

- **接頭辞＋動詞**：`ai*`(125) `render*`(51) `cur*`(36) `audit*`(27) `inv*`(13)
  → 新しい接頭辞を勝手に作らず、既存の家族に合わせる
- **動詞は接尾辞**：`Get` `Set` `All` `Of` `Has` `Modal` `HTML` `Print` `Save` `Row`
  → `getCurSched` ではなく `curSchedGet`
- **業務語はローマ字のまま**：`nichi`(日案) `touen`(登園) `hiyari`(ヒヤリハット) `kujo`(苦情) `kenshu`(研修)
  → `dailyPlan` `nearMiss` と訳すと、園・依頼者と話す言葉とコードがずれます
- **定数は ALL_CAPS**（219件）。書き換わる状態は `DB` `SET` `CUR` `API` の4つだけ

### 字下げ

半角スペース**2つ刻み**。タブは1文字もありません。全角スペースを字下げに使わないでください。
複数行コメントの2行目以降は、親の字下げ＋3スペースに揃えます。

### 括弧の前後にスペースを入れない

`if(` `){` `function(` `for(` と詰めます。

### ★ 全角の括弧をコードに書かない

`（` `）` を**文字列の外**に書くと構文エラーになり、21,000行が死にます。
**2026-09-07 に2回やりました。** 日本語を書いていて指が滑ります。

---

## 2. 出す前に必ず通す検査

### 画面側（cocoas-dashboard）

```bash
node tools/lint.js index.html parent.html
```

```bash
python3 -m http.server 8899
```

```bash
node tools/smoke.js
```

**注意**

- `lint.js` は**引数が必須**です。引数なしだと何も検査しません（終了コード2で止まります）
- `smoke.js` は**先に `http.server 8899` を起動**しておく必要があります。自分では立てません
- **「★文法エラー」が出た回の①②③は信用しないでください。** 文法を直してから流し直します
- 2026-09-07 に、**問題を見つけたら終了コードを落とす**ようにしました。`&& git push` で安全に繋げます

### サーバ側（cocoas-api）

```bash
npm test
```

**★テストは `.env.test` の DATABASE_URL を使います。本番DBを指した状態で絶対に流さないでください。**
テストは開始時にデータを消します。

現在 **513件**。1件も落とさないでください。

---

## 3. 絶対に変えてはいけないもの

| | 場所 | 変えると何が起きるか |
|---|---|---|
| **役職の保存値** `管理者` `マネージャー` `主任` `一般` | index.html `STAFF_ROLES` / server.js `ROLES` | 既存の全アカウントの役職が一覧に無い値になり、**どの画面も開けない人**ができる |
| 役職を足す/減らすとき | **画面とサーバを同じコミットで**直す | 片方だけだとアカウントが作れない、権限が効かない |
| `SYNC.mark` の除外一覧 | `guide_files` `contactPhotos` `staffDocs` `kidDocs` | 「この端末にのみ保存」と画面に書いて集めた**園児の顔写真・職員の資格証**がサーバへ送られる |
| `SYNC.pull` の受け入れ拒否 | `apiBase` `staffAccounts` 等 | サーバ応答1つで**端末の向き先とログイン情報を差し替えられる**＝乗っ取り |
| `KEY_RULES`（server.js） | `rolePerms` `staffAccounts` は管理者のみ | 一般職員が自分に全画面を付与できてしまう |
| `PARENT_WRITE`（parent.js） | 保護者が書ける鍵の一覧 | 園の帳簿を保護者から書き換えられる |
| `API_LIVE` | index.html の元栓 | `false` にすると全端末が即ローカル専用に落ち、**データが消えたように見える** |
| `JWT_SECRET` | Railway の変数 | **全職員・全保護者が同時にログアウト**される。業務時間中は触らない |

**表示名を変えたいときは `ROLE_LABEL` だけを直します**（画面には 園長／主任・リーダー／先生 と出ます）。

---

## 4. 持ち場（2026-09-07 時点）

| 誰 | どこ |
|---|---|
| 奈良さんチーム（Codex） | **`cocoas-api`**（19モジュールに分かれていて並走に向く） |
| Claude Code | **`cocoas-dashboard`**（1ファイルなので1人が持つ） |

`index.html` は 21,629行の1ファイルです。**2人で同時に触ると、文字列置換の編集が失敗します。**
分割の予定はありますが、それまでは持ち場を守ってください。

**触る前に必ず `git pull`。終わったらすぐ push。** 手元に長く抱えないでください。

---

## 5. コミットメッセージ

- **1行目**：日本語で「何をしたか」。体言止めか過去形
- **本文**：**なぜそうしたか**。壊れていたなら `★` で以前の状態を書く
- 末尾に `Co-Authored-By:` を付ける

例：

```
保護者アカウントの行き止まりを3つふさいだ

★止めたアカウントを戻せなかった
  DELETE /parents/:id が active=FALSE にするだけで、戻す口がどこにも無かった。
  卒園して戻ってきた、誤って止めた、で詰む。
  → POST /parents/:id/active
```

---

## 6. 困ったときの調べ方

```bash
grep -n '★' index.html | head -50
```

過去に踏んだ地雷の一覧が出ます。**同じところを踏まないための、いちばん早い方法です。**

システムの全体像は、安田さんに「システム概要書」を求めてください。
